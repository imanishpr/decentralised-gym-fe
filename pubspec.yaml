name: gym_visit_flutter
description: Gym Visit App mobile client for Spring Boot backend.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  dio: ^5.7.0
  go_router: ^14.6.2
  flutter_secure_storage: ^9.2.2
  mobile_scanner: ^7.2.0
  firebase_core: ^3.8.1
  google_sign_in: ^6.2.2
  flutter_facebook_auth: ^7.1.1
  intl: ^0.20.2
  geolocator: ^14.0.2
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
